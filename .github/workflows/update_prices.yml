name: Update Stock Prices

on:
  schedule:
    # Runs every 15 minutes from 7 AM to 11 PM German time (Monday to Friday)
    # German time is UTC+1 (CET) in winter, UTC+2 (CEST) in summer
    # To cover both, we use UTC times that work for both scenarios:
    # 7 AM German time = 6 AM UTC (CET) or 5 AM UTC (CEST)
    # 11 PM German time = 10 PM UTC (CET) or 9 PM UTC (CEST)
    # Using 5 AM to 10 PM UTC to ensure coverage in both time zones
    - cron: "*/15 5-22 * * 1-5"  # Every 15 minutes from 5 AM to 10 PM UTC, Monday to Friday

  workflow_dispatch:  # Allows manual trigger

jobs:
  update_prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Cache Python Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Set up Firebase credentials
        run: |
          # Decode Firebase credentials from base64 secret
          echo "${{ secrets.FIREBASE_CREDENTIALS_JSON_B64 }}" | base64 -d > firebase_credentials.json
          
          # Set Firebase credentials path as expected by your code
          echo "FIREBASE_CREDENTIALS=$(pwd)/firebase_credentials.json" >> $GITHUB_ENV

      - name: Verify Firebase credentials
        run: |
          if [ -f "firebase_credentials.json" ]; then
            echo "✅ Firebase credentials file created successfully"
            echo "File size: $(stat -c%s firebase_credentials.json) bytes"
            # Verify it's valid JSON without exposing content
            if python -c "import json; json.load(open('firebase_credentials.json'))"; then
              echo "✅ Firebase credentials JSON is valid"
            else
              echo "❌ Firebase credentials JSON is invalid"
              exit 1
            fi
          else
            echo "❌ Firebase credentials file not found"
            exit 1
          fi

      - name: Check German Time
        run: |
          echo "Current UTC time: $(date -u)"
          echo "Current German time: $(TZ='Europe/Berlin' date)"
          echo "Job triggered at German time: $(TZ='Europe/Berlin' date +'%H:%M on %A, %B %d, %Y')"

      - name: Run Price Update Script
        run: |
          echo "🚀 Starting price update process..."
          echo "📅 Running at: $(TZ='Europe/Berlin' date +'%H:%M German time (%A)')"
          python updateprice.py
        env:
          PYTHONUNBUFFERED: 1

      - name: Clean up credentials
        if: always()
        run: |
          # Remove credentials file for security
          rm -f firebase_credentials.json
          echo "🧹 Cleaned up credentials file"

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v3
        with:
          name: price-update-logs-${{ github.run_id }}
          path: |
            *.log
            /tmp/*.log
          retention-days: 7

